<!doctype html>
<html lang="fr">
<head>
    <meta charset="utf-8">
    <meta products="viewport" content="width=device-width, initial-scale=1">
    <title>Bonjour</title>
    <link rel="icon" href="/assets/images/favicon.png">
    <link rel="stylesheet" href="/assets/css/styles.css">
</head>
<body>
    <h1> Voici la liste des produits  </h1>
<ul>
        {% for product in products %}
            <li>{{ product }}</li>
        {% else %}
            <li>Nothing to display</li>
    {% endfor %}
    </ul>
    {{ dump(products) }};
</body>
</html>